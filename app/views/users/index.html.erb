<div class="content-wrapper buffer-both text-center">
  <div class="buffer-both">
    <h1 class="title-medium"><%= t('app.headers.our_users') %></h1>
  </div>

  <% @users.each do |user| %>
    <div class="users-list-item">
      <div class="user-image-div user-image-div-small">
        <%= link_to image_tag(user_avatar(user), class: 'user-image'), user_path(user) %>
      </div>
    </div>
  <% end %>
  <div class="hashtags-cloud">
    <div id="tags">
      <% @tags.each do |tag| %>
        <%= link_to "##{tag.name}", tag_path(tag), rel: rand(20)  %>
      <% end %>
    </div>
  </div>
</div>
